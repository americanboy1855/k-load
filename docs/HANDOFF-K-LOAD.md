# HANDOFF: K LOAD — утро 11.09.2026 (раунды 17–21 закрыты; HEAD 8fa4cca)

Проект: `/Users/salaga/Desktop/K/3. SOFT/K DOWNLOADER MAC`
(старый путь `/Users/salaga/Desktop/K/K DOWNLOADER MAC` — заглушка-папка,
shell новых сессий падает туда — cd в реальный проект).
Тесты ядра: `./kdcore/build/kd_tests` — 45 ok (yandex-проверка удалена вместе с Яндексом).
flutter analyze — 0 ошибок. Собрано и запущено: dev-бандл
`~/Library/Application Support/K LOAD dev/K LOAD.app`
(логи: `open ... --stdout /tmp/kload_out.log --stderr /tmp/kload_err.log`).

## Сделано за сессию (детали — git log: 29b3e1f, 78366cd, 0647248, d6a0df4, 8fa4cca)
- Раунд 17: вариантные сигнатуры задач (видео+музыка одного контента
  качаются параллельно), дубликат-плашка починена (была мертва: сравнивали
  mp4 с audioFormat), SC-фильтр DRM в ядре (параллельный полный -J
  кандидатов), пауза-кнопка, «К РЕЗУЛЬТАТАМ», золотые скроллбары.
- Раунд 18: пауза-кнопка слева от СКАЧАТЬ (оранжевый квадрат 34.5,
  иконка = будущее действие), сортировка «актуальное сверху», тонкий
  плейсхолдер 14, К РЕЗУЛЬТАТАМ в строке источника.
- Раунд 19: [ОЧИСТИТЬ] крупная, сортировка с якорем прокрутки,
  КОЛ-ВО без тавтологии + живой СКАЧАТЬ · N, обложка без чёрных полос
  (детект hqdefault 4:3 -> кроп центрального 16:9), VPN-опрос 1 с +
  автопауза загрузок при пропаже VPN + текст «VPN ОТКЛЮЧЁН — …».
- Раунд 20: [ОЧИСТИТЬ] без рамки со скобками, РЕАЛЬНАЯ отмена очереди
  при очистке (Engine::clearFinished: queued -> setCancelled + стереть;
  working/paused живут), компактная VPN-плашка, пиксельные обложки 56px
  (ОТМЕНЕНО в раунде 21), названия строк с маркера @T (сразу, не после
  скачивания; @F уточняет до фактического имени файла).
- Раунд 21 (финал сессии): VPN-плашка гасится плавно и при возврате VPN
  (vpnPlateShown/vpnPlateVisible); пикселизация ОТМЕНЕНА (декод 512 px,
  сглаживание, cover + кроп полос остались); _KModal — единый стандарт
  модалок (затемнение + blur 2.5 как у VPN-плашки, окно по центру, Esc,
  клик по фону, равные кнопки _ModalButton) применён к «ЭТОТ ФАЙЛ УЖЕ
  СКАЧАН» (закрытие анимированное: _dupeClosing + 240 мс); [ОЧИСТИТЬ]
  вплотную к заголовку; Яндекс удалён ПОЛНОСТЬЮ (grep 0 в app/kdcore/
  docs; enum сервисов сдвинулся — serviceLabel: ВК=8, SC=9; strict-путь
  candidateMatches/downloadTrack вырезан); каталог ошибок UserMessage
  {code,title,hint,action} + подсказки на карточке ошибки и в «ПОИСК НЕ
  УДАЛСЯ»; чистка (.DS_Store, случайный скриншот в корне);
  docs/PKG-PLAN.md — поэтапный план .pkg (сборку НЕ начинать, есть
  открытые вопросы: бандлить ли инструменты, universal?, нотаризация?).

## Незавершённое / не проверено живьём
- _KModal (дубль): код новый, визуально с затемнением НЕ снят; Esc-закрытие
  не проверено. Ду́пла добиться легко: скачать что-то -> вставить ту же
  ссылку -> СКАЧАТЬ.
- VPN: плавное гашение при возврате и автопауза при пропаже не проверены
  живьём (нельзя переключать системный VPN); механика паузы/докачки
  проверена ранее E2E байт-в-байт.
- Аудит-хвосты: нет места на диске, нет прав записи, 50+ загрузок подряд,
  ресайз окна — не гонялись.
- Скриншоты: `~/Desktop/K LOAD скрины` — 16 шт (01–16, имена = состояния).
  НЕТ кадров: КАЧАЮ/пауза (сеть слишком быстрая — файлы качаются за
  секунды), VPN-плашка, модалка дубликата с затемнением, hover на
  [ОЧИСТИТЬ]/К РЕЗУЛЬТАТАМ.

## Выключение (из прошлого задания)
Было запланировано авто-выключение — НЕ сработало: машина включена,
процесса нет, лог /tmp/kload_shutdown.log пуст. Всё сохранено: git чист.
Если выключение всё ещё нужно — сделать вручную.

## Грабли (пополнил — важно!)
- CUA-автоматизация Flutter: нестабильна из-за борьбы за фокус (пользователь
  работает параллельно). Рабочие рецепты: open_application(activate=true) ->
  клик по координатам поля (280,90) свежего кадра -> type app_ref;
  cmd-чорды НЕ доходят; delete и текст доходят только при фокусе поля;
  AXPress по свежим токенам для чипов ~50/50 — лучше координаты;
  крестик [×] из протухшего состояния срабатывает ЗАДЕРЖАННО и стирает
  ввод (гонка!) — после × ждать 2 с и проверять поле через AX value.
- Скриншоты: `screencapture -x -o -l <window_id>` (не крадёт фокус);
  быстротечные состояния — фоновый цикл скринов ДО действия.
- Сеть у пользователя очень быстрая: «КАЧАЮ…» живёт секунды; для
  устойчивых состояний качать длинные видео (плейлист lofi — видео
  по 1+ часу) или свежие (нескачанные) файлы.
- dev-build.sh глотает падение codesign (iCloud-xattr): flutter build
  может упасть — проверять маркер-строку в kernel_blob.bin, затем
  снова dev-build.sh; после замены libkdcore.dylib — codesign.

## Предложенные скиллы следующему агенту
- Skill `handoff` — при следующей передаче.
- Skills `pixel-compare` (сверка скриншотов), `impeccable` / `webcore`
  (UI-правки в пиксельной стилистике), `find-skills` — при новых типах задач.

---

# ИСТОРИЯ (раунды 1–16 и ранее) — исходный хэндоф

# HANDOFF: K LOAD — состояние на 08.09.2026 (поздний вечер, сессия «сборка приложения»)

Проект: `/Users/salaga/Desktop/K/K DOWNLOADER MAC` (имя папки старое — не менять).
Полный контекст: `docs/HANDOFF-K-LOAD.md` и `docs/PLAN.md`.

## Главное этой сессии: приложение собрано и работает

1. **Правки макета (design/main-screen.html)** по замечаниям заказчика:
   - кнопка «$» — вдавливание как у папки (JS-хак удалён; чинился и её
     transform: `translate(0,-50%)` при `right:22px`);
   - нижний бэнд: ось контента на 48px — зазор от стекла = зазору до низа рамки;
   - ХРОН: настоящий поповер «ОТ/ДО» (editable, нормализация М:СС на blur),
     открывается чипом, прячется у подборок; `&chron=1` — для скриншотов;
   - автопереключение на МУЗЫКУ для музыкальных сервисов (ВИДЕО скрыт);
   - `[Ссылка]` возле бейджа источника (мелкий Handjet, в скобках, открывает браузер);
   - починен тост папки (элемент `#toast` отсутствовал в HTML — клик падал).
2. **kdcore**:
   - папка по умолчанию теперь `~/Downloads/K LOAD` (было «K DWNLD»);
   - ХРОН в ядре: опция `"sections":"10:00-10:20"` → `--download-sections` +
     `--force-keyframes-at-cuts`; у плейлистов игнорируется; видна в снапшоте;
   - контрактные тесты 32 ok; live-тест хрона: файл ровно 10.03 сек (проверено
     и в приложении). YouTube-live тесты падают без VPN — известная грабля.
3. **Приложение (app/)** — каркас-заглушка заменён на настоящий UI по макету:
   - `lib/ui/theme.dart` — палитра/типографика/пунктир; `lib/ui/widgets.dart` —
     иконки, LED-полосы, сканлайны, луч boot, круглые плашки;
     `lib/ui/screen.dart` — экран целиком; `lib/kd/kd_bindings.dart` — +probe
     поля, +sections.
   - Окно: `MainFlutterWindow.swift` — 560×670, contentAspectRatio (ресайз
     только по диагонали, 0.8×–1.4×), зум отключён, фуллскрин запрещён,
     тайтлбар прозрачный (родной светофор поверх нашей панели).
   - Работает end-to-end (проверено живьем в приложении): boot-клик и луч,
     ПОИСК→НАЙДЕНО (реальный probe, живая обложка), автоМУЗЫКА, ХРОН-поповер,
     скачивание отрезка (10.03 с!), папка `~/Downloads/K LOAD` (создается,
     открывается Finder), мусорка удаляет файл С ДИСКА, `[Ссылка]`.
   - Шрифты и логотип: ассеты из design/ (`app/assets/`), логотип —
     `assets/kvartal.svg` через flutter_svg (чистый Dart, без нативщины).
4. **Имя и иконка**: PRODUCT_NAME = «K LOAD» (бандл `K LOAD.app`, dev-build.sh
   обновлён), иконка — тёмный пластик + янтарное «K» (Handjet wght=800) +
   LED-полоса 3/5; генератор `app/scripts/gen_icon.swift` (swift scripts/… из app/).
5. **Сборка/запуск**: `cd app && ./scripts/dev-build.sh` →
   `~/Library/Application Support/K LOAD dev/K LOAD.app`.
   Падение codesign внутри `flutter build` — ожидаемо (iCloud); скрипт
   переподписывает вне синхронизируемой зоны.

## Дизайн: как работать (не изменилось)
- Правда — design/main-screen.html; сервер: `cd design && python3 -m http.server 8787`
  (сейчас запущен). Состояния: `#boot=skip&vpn=on&flow=seek|found|queue&sc=…&chron=1`.
- Галерея: design/gallery.html. share/kload-cli.sh (8790) не трогать.

## Следующие шаги (черновик, согласовать с заказчиком)
1. Заказчик гоняет приложение, собирает правки по интерфейсу.
2. Доработки после прогона: выбор папки назначения (сейчас папка только
   открывается), drag-out файла из плашки (удержание), качество/аудиоформат
   (чипы из блоков 3 плана), VPN-плашка если VPN реально выключен.
3. Финальный URL логотипа (заглушка kvartalrecords.ru) — заменить в макете
   и в `lib/ui/screen.dart`.
4. Бэклог ядра: nothing new; этап 3 (плагин) и этап 4 (подпись/нотаризация).

## Грабли (пополнил)
- AX `set_value` во Flutter TextField НЕ вызывает onChanged — только
  клавиатурный ввод. Для автотестов: click по полю + `type` app_ref.
- `#filePath` в `swift scripts/x.swift` — относительный путь; в генераторе
  иконки пути от cwd (запускать из app/).
- `NSWindow.contentAspectRatio` не меняет начальный кадр — задавать
  `setContentSize` явно.

## Окружение (не изменилось)
Flutter 3.47.2, Xcode 26.6, CMake+Ninja; CocoaPods не нужен (flutter_svg без
нативного кода). Git: main, всё закоммичено этой сессией.

## Доработка после прогона заказчика (c9403ed)

Заказчик прогнал приложение и показал два скриншота (макет vs живое). Что
вскрылось и починено:

- **kd::upToFirst без маркера возвращал пустоту** — любая однострочная ошибка
  yt-dlp доходила до человека как безликое «Загрузка не удалась». Теперь
  honest-текст («Запись защищена от скачивания» для DRM и т.д.). Тест добавлен.
- АвтоМУЗЫКА (пункт 4) не срабатывала для ПОИСКА ПО НАЗВАНИЮ: сервис искали по
  p.link (= сырой текст), а надо по resolved. Теперь SoundCloud-результат
  сразу включает МУЗЫКУ и прячет ВИДЕО.
- [Ссылка] (пункт 5) для поиска скрывалась: src брался из p.link (не http).
  Теперь первый http из resolved/link/текста.
- Обложки поисковых результатов: у flat-playlist -J поле thumbnail часто
  пустое — добираем thumbnails[]/artwork_url (SoundCloud → t500x500).
- Корзина у упавших строк (убрать), крестик у ожидающих; повторный СКАЧАТЬ
  не дублирует ссылку, уже стоящую в очереди/работе; в строке очереди
  показываем название из разбора вместо сырого URL.
- Грабли: AX set_value во Flutter не триггерит onChanged (только клавиатура);
  контрольный сценарий проверки — «inmywhitee» (DRM-трек) и Flickermood (ок).

## Правки заказчика, раунд 2 (676fd07)

- Логотип kvartal: вдавлен в пластик (3 слоя SVG: тёмная кромка сверху,
  светлая снизу, основной), на 3px ниже — оптически на линии кнопок; ховер —
  янтарное свечение (бренд: «лёгкое свечение drop-shadow» разрешено).
- Папка: тёмная как «$», ховер — свечение; механизм PlateHover (InheritedWidget)
  прокидывает ховер плашки в CustomPaint-иконки.
- Хрон-поповер: В СТРОКЕ режимов между ХРОН и СКАЧАТЬ (диспетчер загрузок не
  перекрывает), подсказка «ОТРЕЗОК · …» убрана, поля ужаты до 56px.
- DRM: распознаётся при РАЗБОРЕ (yt-dlp -J упал + «drm» в слитом stdout;
  ChildProcess сливает stderr в stdout). Карточка из oEmbed (title/thumbnail),
  флаг probe.drm, вместо режимов — «ФАЙЛ ЗАЩИЩЁН DRM — СКАЧАТЬ ЕГО
  НЕВОЗМОЖНО» красным со свечением. soundclouddl.cc проверен: DRM не обходит
  (обычный загрузчик публичных треков, API по почте) — интеграции не будет.
- «ТОЛЬКО ЗВУК» из хронометража убран (и в макете).
- Плавание экрана убрано (и в макете). Boot: луч сразу при запуске, без
  «НАЖМИ, ЧТОБЫ ВКЛЮЧИТЬ».
- Рамка обложки цельная (DashedBorderPainter(solid:true)), трекинг 8px.
- Корпус до системной рамки окна: LayoutBuilder cover-масштаб, радиус 10.5,
  Scaffold/окно пластиковые (#161413) — микрощели на углах убраны.
- Свечение текста в стиле: название карточки, % поиска, ГОТОВО, VPN-строка.
- Грабли: Flutter MouseRegion не ловит фоновый mouse_move CUA — для ховер-
  тестов нужен реальный курсор (Quartz CGEventPost), и то если окно сверху.

## Правки заказчика, раунд 3 (673afb0)

- ХРОН — раскрывающийся: панель ОТ/ДО под чипом (позиция чипа замеряется
  GlobalKey + postFrame), AnimatedSize 240ms раздвигает контент (очередь
  уезжает вниз, ничего не перекрывается). Закрытие: повторный клик по ХРОН
  или клик вне панели (_ui обёрнут в translucent GestureDetector). Инлайн-
  блок в строке убран — RIGHT OVERFLOW на СКАЧАТЬ исчез.
- Папка назначения: круглая кнопка -> NSOpenPanel через MethodChannel
  «kload/native» (MainFlutterWindow.swift). Выбранная папка = dest будущих
  загрузок (передаётся в enqueue), тост с путём; MissingPluginException ->
  сообщение об ограничении. В строке очереди кнопка папки по-прежнему
  открывает папку файла.
- LED-полоса очереди: AnimatedLedRow — плавно догоняет прогресс (900ms
  easeOutCubic), ведущий сегмент слабо подмигивает (1.1s) — «нестабильная
  индикация ВИДЕОМАГНИТОФОНА».
- Текст резкий: сканлайны/виньетка/блик перенесены ПОД контент
  (GlassBackdrop), над контентом только мерцание + аналоговый шум
  (GlassVeil, зерно 2x1 alpha .03, смена 160мс).
- Ховеры: чипы — заливка amber .14 + glow .20 (200ms); СКАЧАТЬ — glow .25->
  .45/22, press = translateY 1px + scale .98; круглые плашки — внешнее
  свечение amber .30/18; поле поиска — TweenAnimationBuilder 320ms
  easeOutCubic (фокус 1.0, ховер .35). Папка в строке очереди янтарная,
  на ховере свечение (_GlowWrap). Лого на 1px ниже (top:52).

## Правки заказчика, раунд 4 (финал сессии)

- $ иконка тёмная как папка (Builder под PlateHover), ховер — фосфор; свечение
  плашек смягчено (amber .18/12). Tooltip'ы убраны ВЕЗДЕ (Plate, _actButton).
- Иконки очереди: папка и корзина светло-оранжевые (Pal.amber), ховер —
  Pal.soft без blur/геометрии (_actButton принимает builder(hover)).
- Чипы ВИДЕО/МУЗЫКА/ХРОН: один AnimatedContainer (180ms easeOut), без
  AnimatedScale/Opacity-скачков; текст через AnimatedDefaultTextStyle.
- ХРОН + MP3: ЯДРО ПРОВЕРЕНО ПОЛНОСТЬЮ (видео 5.0с, YT-MP3 5.04с, SC 10.03с,
  +nameOverride/кириллица 5.04с) — ядро исправно. Реальный баг был в UI:
  клик мимо панели ВЫКЛЮЧАЛ режим. Теперь chronOpen (панель) отделён от
  chronOn (режим): закрытие панели не сбрасывает отрезок. E2E: 10.03с после
  скрытия панели.
- Boot: луч один раз (повторный _finishBoot защищён), шум/мерцание переехали
  внутрь GlassVeil (свой таймер/контроллер — экран не перестраивается),
  reduced-motion: мгновенное включение, атмосфера выключена.
- Текстовые тени убраны (Handjet просвечивал тени как «микропалочки»);
  лого top:55 (оптически на линии кнопок), кромки deboss мягче.
- Трекинг: 3.2s раз в 18-30с. CRT: края экрана мягко вгибаются внутрь
  (RRect-тень в GlassBackdrop), ничего не обрезает.

## Правки заказчика, раунд 5 — шрифты, источники, плейлисты, качество, плазма

- Шрифты: Press Start 2P (акценты/кнопки/ярлыки, CAPS) + Departure Mono
  (ввод, названия, метаданные, ссылки) — оба из webcore-ассетов с OFL;
  в theme.dart роли T.ps/T.mono. Handjet остался только в логотипе-плашке.
- Источник поиска: kd_probe_async_source (Engine::probe text+site,
  probeSearch фильтрует youtube/soundcloud). UI: бейдж-дропдаун АВТО/
  YOUTUBE/SOUNDCLOUD для текстовых запросов, смена источника переразбирает
  запрос. Для ссылок дропдауна нет; пачка с одного сервиса — его бейдж.
- Пачка: «ВИДЕО + ФОТО + МУЗЫКА» убрано; последовательный сбор метаданных
  (probeAsync по очереди) → «ОБРАБОТАНО X/Y» → «ХРОНОМЕТРАЖ · h:mm:ss».
- Плейлисты: ядро суммирует длительности entries (flat -J), Options.
  playlistLimit -> --playlist-items 1:N. UI: КОЛ-ВО чип вместо ХРОН,
  панель с числовым полем (пусто = весь), СКАЧАТЬ · N живо, ВИДЕО/МУЗЫКА.
- Качество: чип для видео (высоты из probe + МАКСИМУМ), по умолчанию 1080;
  без известных высот — подпись МАКС (не интерактивна).
- Обложка: _SmartCover — дизер-плазма (Байер 4x4, 4 тона амбры, зерно 6px,
  степ 90мс) вместо шахматки; кроссфейд на настоящую обложку; без арта
  плазма живёт постоянно.
- Ховеры: _ActButton — самостоятельный stateful (ховер каждой кнопки свой),
  папка/корзина amber -> soft; чипы без scale/Opacity (один AnimatedContainer
  + AnimatedDefaultTextStyle) — уход мыши плавный.
- Панели (хрон/КОЛ-ВО/качество/источник): GlitchIn — проявление + сдвиг 3px
  + горизонтальные помехи, 280ms; клик мимо закрывает (режим хрона не сбрасывает).
- Иконка: gen_icon2.swift — гранжевая k из Фавикон.png (DeviceGray-маска,
  клип + янтарный градиент), бейдж-круг загрузки с золотым градиентом справа
  снизу; все размеры app_icon_*.png перезаписаны.
- Проверено живьём: запрос+flickermood (YOUTUBE->SOUNDCLOUD переразбор),
  плейлист 200 видео/12:54:06 без переполнений, КОЛ-ВО панель, плазма.

## Правки заказчика, раунд 6 (финал)

- Boot: BootBeamPainter рисует чёрный фон ВСЕГДА (раньше фон был прозрачным и
  интерфейс был виден до луча); UI проявляется AnimatedOpacity 450ms после
  луча; reduced-motion — мгновенное включение без атмосферы. Проверено
  скриншотами: чёрный экран (1.35с) -> вспышка (1.85с) -> интерфейс.
- Placeholder сокращён: «Вставьте ссылку или напишите название».
- Ховеры без прыжков на выходе: у AnimatedContainer списки boxShadow
  постоянной длины (alpha 0 в покое) — чипы, _ActButton, круглые плашки.
- Качество: перенесено под бейдж источника (компактный блок YOUTUBE /
  1080 P v); МАКСИМУМ + реальные высоты из probe.heights; без известных
  высот или для музыки — подпись МАКСИМАЛЬНОЕ. Из ряда режимов чип убран.
- Источник: внутри дропдауна поиск по названию (TextField-фильтр).
- Пачка: компактные метки найденных сервисов (svcNames, PS2P 7).
- VPN: текст «ДЛЯ ЛУЧШЕЙ РАБОТЫ ПРИЛОЖЕНИЯ — ВКЛЮЧИТЕ VPN»; BackdropFilter
  (блюр 2.5) + затемнение только внутри стекла; плашка GlitchIn + Scale,
  тап по плашке не закрывает (поглощение), крестик/скрим закрывают;
  IgnorePointer при скрытии — нет click-through; vpnEpoch — новый glitch на
  каждом переходе вкл->выкл; появление через 1с после включения ТВ.
- Иконка: gen_icon2.swift — белая гранжевая k на весь тайтл (DeviceGray-маска
  из Фавикон.png), оранжевая ПИКСЕЛЬНАЯ стрелка вниз без круга (сетка 5x6,
  перевёрнута по Y), свечение стрелки.
- Проверено живьём: чёрный кадр до луча, вспышка, прямая YouTube-ссылка
  (качество 1080 P v -> МАКСИМУМ/240/144 реальные высоты), плейсхолдер.

## Правки заказчика, раунд 7 — форматы, каталоги поиска, плейлисты по-роликово

- Ядро: AudioFormat::ogg (vorbis); контейнер видео mp4/webm/mkv (Options.
  container -> --merge-output-format); формат фото jpg/png (конвертация
  штатным sips после скачивания); поиск по каталогам: Apple Music (офиц.
  iTunes Search API), Spotify (анонимный токен веб-плеера + api.spotify.com),
  Pinterest (закрыл автопоиск 403 — честная ошибка). kd_http::fetch с
  заголовками (Bearer); kd::searchRegex.
- Плейлисты: startPlaylist — плоский перебор (-J --flat-playlist) и качание
  ПО РОЛИКАМ: недоступные пропускаются («Готово · файлов: 2 · пропущено: 3»),
  прогресс «Качаю i из N», папка плейлиста с нумерацией 01-/02-, лимит
  (playlistLimit) обрезает список. Прежний путь с --yes-playlist падал целиком
  от одного недоступного ролика.
- Проверено живьём: плейлист 200 видео -> лимит 5 -> 2 файла + 3 пропущено;
  Apple (Forss - Flickermood, 210с), Spotify/Pinterest —
  честные ошибки при недоступности.

## Правки заказчика, раунд 8 (финал сессии)

- Поиск: только downloadable-источники (Pinterest убран — закрыл автопоиск,
  работает по ссылке); SoundCloud: flat scsearch5 -> проверка первых 3
  кандидатов полным -J (formats пусто/ошибка = DRM/закрыт) -> первый
  скачиваемый; fallback-ошибка больше не перезаписывает DRM-сообщение.
- Drag-out: нативный DragOutHelper (MainFlutterWindow.swift): Dart шлёт зоны
  готовых строк («setDragZones», дизайн-координаты -> contentView через
  cover-scale), локальный monitor leftMouseDown начинает NSDraggingSession
  с реальным файлом (NSURL), нажатие поглощается. Курсор grab на готовых.
- Качество: если высоты неизвестны — подпись «· МАКСИМАЛЬНОЕ КАЧЕСТВО»
  рядом с форматом (MP4 · МАКСИМАЛЬНОЕ КАЧЕСТВО).
- Иконка: маска 28x28 (грубые пиксели), K на весь тайтл (8,8,1008,1008),
  стрелка cell 72 на тёмном пиксельном вырезе в правом нижнем углу.

## Правки заказчика, раунд 9 (финал)

- Список результатов текстового поиска: Probe.results (до 5), сериализация в
  CAPI, UI-список в карточке (GlitchIn), выбор строки -> probeAsync(url) ->
  полный разбор (высоты/превью/форматы как у прямой ссылки); «К РЕЗУЛЬТАТАМ»
  возвращает к списку; недоступный результат помечается ошибкой на строке.
- YT HTML-ветка: findAllVideoRenderers — все результаты выдачи, не только
  первый; ytsearch5 flat: url/webpage_url.
- Кэш разборов: Engine::probeCache (текст+сайт -> Probe, TTL 5 мин, лимит 48).
- Drag-out: DragOutHelper/DragSourceView в Runner — нативные прозрачные
  NSView-зоны по прямоугольникам готовых строк (Dart шлёт «setZones»);
  mouseDown -> beginDraggingSession с NSURL файла (copy), нажатие
  поглощается; зона без правых кнопок (w-84), hover-подсветка + pointingHand.
- Иконка: маска 28x28 (грубый пиксель), K (8,8,1008,1008), стрелка cell 72
  на тёмном вырезе.

## Правки заказчика, раунды 10–16 (10.09.2026) — кратко; детали в /tmp/HANDOFF-K-LOAD.md и git log

- Раунды 10–15: превью из кэша ядра, панель результатов-агрегатор,
  ID (потом удалён), единые форматы IG/TikTok, плейлисты по-роликово и
  отдельными задачами, честные статусы/ошибки, дубликат-диалог, deno.
- Раунд 16: очередь на ДВУХ воркерах + глобальная пауза. Wake — считающий
  семафор (одноразовый флаг при двух воркерах терял пробуждения и вешал
  деструктор). kd_set_paused/kd_is_paused; yt-dlp при паузе получает SIGTERM
  и корректно закрывает .part — снятие паузы докачивает с позиции. В паузе
  воркеры спят по 200 мс (снятие не зависит от жетонов Wake). clearFinished
  бережёт paused, отмена paused — сразу, деструктор сигналит каждого воркера.
  UI: квадратная кнопка паузы в диспетчере, активные сверху (_sortedItems).
  Тест testPauseResume на фейковом yt-dlp (имя стаба yt-dlp_macos!) — сюита
  46 ok; живой E2E: пауза заморозила загрузку, снятие докачало до «Готово».
- Папка проекта переехала в `3. SOFT/`; по старому пути оставлена
  папка-заглушка с `.keep` для сессий со старым рабочим каталогом.
