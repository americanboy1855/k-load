# K LOAD (Windows) — журнал установки, тестирования и исправления

Дата: 14.09.2026 · Система: Windows 10 Pro 22H2 x64 · Имя пользователя: **Женя** (кириллица в профиле)
Репозиторий: https://github.com/americanboy1855/k-load · HEAD main `5226435`

## Итог

Приложение собрано, все найденные баги **исправлены в коде**, протестировано повторно
и установлено на компьютер. Ярлык «K LOAD» на рабочем столе.

- Исправлений в коде проекта: **8** (ERR-01, 02, 06, 07, 08, 09, 13, 14 — ниже)
- Тесты после фиксов: ядро 45/45 ✅, Flutter 7/7 ✅, живые скачивания ✅ (кроме YouTube,
  который без VPN временно требует «проверку не робот» — ограничение самого YouTube)
- Установка: `C:\dev\k-load\dist\K LOAD\` (портативная, 396 МБ) + ярлык на рабочем столе

---

## Часть 1. Что было сломано и как исправлено (все правки в коде)

### FIX-1 (был ERR-06) · Битый путь профиля → скачивания «исчезали»
- **Было:** `DestResolver::homeDir()` и `appDataRoot()` брали `getenv("USERPROFILE"/"APPDATA")`
  (байты CP1251) и читали их как UTF-8 → папка назначения = `C:\Users<мусор>\Documents\K LOAD`,
  журнал ядра молча не писался.
- **Стало:** `GetEnvironmentVariableW` (wide-API) в обоих местах.
- **Проверено:** журнал пишется в `%APPDATA%\K LOAD\engine.log`; dest в снапшоте — корректный.

### FIX-2 (был ERR-08) · Ядро не мог запустить yt-dlp на этой машине
- **Было:** на этом компьютере любой `CreateProcessW` с явным env-блоком возвращает ошибку
  (87/183) — системная аномалия, проверено минимальным репро на yt-dlp и cmd.exe.
  Ядро всегда спавнило yt-dlp с env-блоком → загрузчик не стартовал вообще.
- **Стало:** при неудаче — повтор спавна с наследованием окружения (nullptr).
- **Проверено:** живые тесты: yt-dlp запускается, «завершён: код 0», файлы скачиваются.

### FIX-3 (был ERR-09) · «Процесс не запустился» маскировался под «контент недоступен»
- `captureOut`/`runYtDlp` теперь пробрасывают код ошибки спавна; в UI отдельная карточка
  «ЗАГРУЗЧИК ЗАБЛОКИРОВАН — Система не пустила yt-dlp, проверьте антивирус»
  (каталог сообщений расширен с 13 до 14 позиций, тест обновлён).

### FIX-4 (был ERR-13) · ffprobe/ffmpeg из ядра никогда не запускались на Windows
- **Было:** ядро зовёт `tools/ffprobe` без расширения; `CreateProcessW` не добавляет `.exe`,
  если в имени есть путь → err=2 → проверка аудио/ХРОН всегда «0 секунд».
- **Стало:** `resolveExecutable()` в `ChildProcess::start` подставляет `.exe`.
- **Проверено:** «soundcloud отдал mp3» и «отрезок меньше целого ролика» стали ok.

### FIX-5 (был ERR-14) · Двойная блокировка мьютекса → крах 0xC0000409
- **Было:** в трёх ветках `finish()` вызывался под уже взятым `mutex` (второй lock
  non-recursive мьютекса = UB). Срабатывало, когда проверка аудио не проходила.
- Где: «Аудио не удалось обработать», «Обрезка не удалась» (Pinterest), финал плейлиста.
- **Стало:** лок только вокруг работы с данными, `finish()` — снаружи.
- **Проверено:** прогон без краха; раньше падало детерминированно на SoundCloud-треке.

### FIX-6 (был ERR-15) · Зависание при уничтожении движка (гонка Wake-токенов)
- **Было:** `wake.waitForever()` в workerLoop: если воркер был занят в момент сигнала,
  его токен съедал соседний воркер — при `kd_engine_destroy` воркер засыпал навсегда, join висел.
- **Стало:** `wake.waitMs(500)` с проверкой quit (аналогично в ProbeRunner — 300 мс).
- **Проверено:** прогон завершается сам, «итого» печатается.

### FIX-7 (был ERR-07) · Живые тесты строили невалидный JSON
- Пути Windows с «сырыми» бэкслешами ломали JSON → опции (dest/mode/quality) тихо
  отбрасывались. Добавлен `jsonPath()` (экранирование `\` и `"`).

### FIX-8 (были ERR-01/02) · Скрипты не работали в штатном PowerShell
- `win/fetch-tools-win.ps1` и `win/build.ps1` пересохранены в UTF-8 **с BOM**;
  `build.ps1` теперь ищет vcpkg в `%LOCALAPPDATA%\vcpkg`, `C:\vcpkg`, `C:\dev\vcpkg`.
- **Проверено:** PS 5.1 парсит оба скрипта с 0 ошибок (раньше — ParserError).

## Часть 1б. Правки поfeedback пользователя (15.09.2026... 14.09, вторая итерация)

### FIX-9 · VPN не определялся (прокси-VPN) → загрузки автоматически ставились на паузу
- **Было:** VpnMonitor (Windows) считал VPN включённым только при TUN/PPP-адаптере или
  имени адаптера «tun/tap/wg/vpn/…». Прокси-VPN (Happ/v2rayN/Clash — локальный прокси
  127.0.0.1:10809 без TUN-адаптера) не распознавался → плашка «VPN ОТКЛЮЧЁН» и
  **авто-пауза каждой загрузки** (механика «VPN пропал → пауза» срабатывала постоянно) —
  «скачивание не работает вообще».
- **Стало:** если системный прокси WinINET включён (ProxyEnable=1) — VPN считается
  включённым. Дополнительно libcurl ядра теперь ходит через системный прокси
  (`kd_http.h: systemProxy()`); yt-dlp читает реестровый прокси сам (urllib).
- **Проверено:** плашка больше не появляется при включённом Happ; авто-паузы нет.

### FIX-10 · Безрамочное окно, кнопки окна в правом верхнем углу
- **Было:** Windows 10 сам добавлял WS_CAPTION к стилю окна (WS_SYSMENU/WS_MINIMIZEBOX
  тянут его за собой) → стандартный заголовок с белой рамкой рисовался поверх клиента;
  собственные кнопки «– ×» висели в правом нижнем углу.
- **Стало:** WS_SYSMENU/WS_MINIMIZEBOX убраны из стиля, WS_CAPTION срезается после
  создания (+guard в `ViewProcThunk`: Flutter-view всегда в (0,0) клиентской области —
  найден сдвиг view при старте); кнопки «– ×» перенесены в правый верхний угол
  (по макету), клики работают (chromeRects пересчитываются автоматически).
- **Проверено:** визуально — окно без рамки, кнопки сверху справа; клик «×» закрывает
  приложение; перетаскивание за верхнюю полосу и ресайз краёв сохранены.

## Часть 1в. Полное тестирование источников и правки от 15.09

### FIX-11 · Кнопки «МУЗЫКА»/«ХРОН» «не нажимаются»
- **Расследование (systematic-debugging):** инструментировал тапы (C:\Temp\kload-taps.log).
  Тап по «МУЗЫКА» **доходит и срабатывает**, но через долю секунды событие повторного
  разбора сбрасывало `mode = 'video'`: `_onProbe` при каждом событии разбора
  перезатирал режим/ХРОН/качество. Плюс была найдена моя же регрессия FIX-6:
  `wake.waitMs(300)` в ProbeRunner заставлял его переобрабатывать тот же запрос
  каждые 300 мс — шторм событий разбора.
- **Стало:** повторный разбор той же ссылки обновляет карточку, но не трогает выбор
  пользователя (режим/ХРОН/качество) — сброс только на новой ссылке
  (`_modeAutoLink`); ProbeRunner возвращён на `waitForever` (+ проверка quit
  после обработки).
- **Проверено:** клик «МУЗЫКА» — подсветка переключилась и осталась; «ХРОН» —
  открылась панель ОТ/ДО, формат переключился на MP3.

### FIX-12 (новый баг ядра) · yt-dlp печатает пути в CP1251 → маркер @F отклонялся
- **Симптом:** скачивание в папку с кириллицей проходило, файл на диске появлялся,
  но строка задания оставалась без файлов (файл «не считался скачанным»).
- **Причина:** stdout yt-dlp в пайпе — в локальной кодировке CP1251; ядро ждёт UTF-8.
  PYTHONUTF8=1 не помогает (yt-dlp перекодирует сам) — помогает его флаг
  `--encoding utf-8`, добавлен во все 8 мест запуска.
- **Проверено:** VK-файл с кириллическим именем — маркер @F принят, задание «done».

### Тест-стенд
- В приложение добавлен автотест-хук (только при env `KLOAD_AUTOTEST_URL`):
  автоподстановка ссылки (тот же путь, что ручной ввод) и автонажатие СКАЧАТЬ
  (`KLOAD_AUTOTEST_GO=1`). Позволил прогнать все источники детерминированно.

### Матрица скачиваний (все через UI приложения, файлы проверены на диске)

| Источник | Ссылка | Результат |
|---|---|---|
| SoundCloud | soundcloud.com/forss/flickermood | ✅ Flickermood.mp3 |
| VK Видео | vkvideo.ru/video-93654338_456239537 | ✅ mp4 (кириллическое имя) |
| TikTok | tiktok.com/@tiktok/video/7106594312292453675 | ✅ mp4 |
| Instagram | instagram.com/reel/DaikHQxo_Z3/ | ✅ mp4 |
| Pinterest (фото) | pinterest.com/pin/1004795366878354024/ | ✅ track.jpg |
| YouTube | youtube.com/watch?v=jNQXAC9IVRw | ✅ Me at the zoo.mp4 |
| Spotify | open.spotify.com/track/4uLU6hMCjMI75M1A2tKUQC | ✅ mp3, найден на YouTube, теги вшиты |
| Apple Music | music.apple.com/ru/album/1440833098 | ✅ весь альбом: 18 mp3 The Beatles с тегами |

### Прочее проверено интерактивно
- Дубликат-модалка «ЭТОТ ФАЙЛ УЖЕ СКАЧАН» (ОТКРЫТЬ В ПАПКЕ / СКАЧАТЬ ЕЩЁ РАЗ / ОТМЕНА) ✅
- Кнопки окна «–»/«×» в новом месте ✅ (клик «×» закрывает приложение)
- Кнопка папки → нативный диалог выбора папки ✅
- Безрамочное окно: перетаскивание за верхнюю полосу, ресайз краёв ✅
- VPN-определение при прокси-VPN: плашка не появляется, авто-паузы нет ✅

## Часть 1г. Правки по репорту пользователя от 16.09 (полная диагностика)

### FIX-17 · Кнопки Kvartal/$/папка-строки/ССЫЛКА не открывали ничего
- **Причина:** `_openPath` вызывал macOS-команду `open`; на Windows её нет,
  исключение глоталось — все внешние ссылки молча не срабатывали.
- **Стало:** Windows → `explorer.exe` (открывает и сайты в браузере, и папки).
- **Проверено:** [ССЫЛКА] → браузер открыл YouTube; кнопка папки на строке →
  Проводник открыл «Загрузки\K LOAD» (окно с заголовком K LOAD).

### FIX-14 · Превью никогда не скачивались (плазма на карточке)
- **Причина (доказана логом):** в `cachedThumbnail` путь кэша передавался
  `downloadToFile` как `Str` — неявное `Str→fs::path` на MSVC читает байты
  в CP1251, и `%APPDATA%` с кириллицей превращался в `Р–РµРЅСЏ`: `_wfopen`
  падал, файлы кэша не создавались вообще (кэш был пуст от начала времён).
- Попутно найден UB: `Sink{text,file}` в downloadToFile с мусорным `text`.
- **Стало:** явный `kd::u8path` (скачивание и rename), sink обнулён.
- **Проверено:** `kd_thumb_path` возвращает путь, файл в кэше, обложка
  «Me at the zoo» отображается на карточке визуально.

### FIX-15 · «Проверяю файл…» висел вечно, очередь казалась мёртвой
- **Причина:** `probeFileDuration` крутил read-цикл без общего дедлайна —
  зависший ffprobe держал воркера навсегда; отмена/очистка не помогали
  (строка не покидала working).
- **Стало:** дедлайн 15 с (kill ffprobe); таймаут ≠ битый файл —
  не удаляется, задание завершается. Прогон SoundCloud mp3: проверка
  и «Готово» за секунды.

### FIX-16 · «Готовлюсь…» без обратной связи минутами
- **Стало:** перед запуском yt-dlp строка показывает
  «Подключаюсь к источнику…» (в бою замечено на строке плейлиста).

### Прогон всех режимов скачивания (через UI приложения)

| Режим | Источник | Результат |
|---|---|---|
| ВИДЕО | YouTube (Me at the zoo) | ✅ mp4 |
| МУЗЫКА | SoundCloud | ✅ mp3 + быстрый «Готово» |
| ХРОН | YouTube 0:00–0:10 | ✅ «[00#00–00#10].mp4» за 4 с |
| ПЛЕЙЛИСТ ≤5 | YouTube (183 видео, КОЛ-ВО=5) | ✅ 3 скачано (2 реально удалены с YouTube — честные «КОНТЕНТ НЕДОСТУПЕН»), нумерация 01–05 |

### Протыканные кнопки (вживую, клик → эффект)
- Чипы ВИДЕО/МУЗЫКА/ХРОН/КОЛ-ВО ✅ (панель ОТ/ДО открывается)
- КОЛ-ВО: панель + ввод лимита через автотест-хук (в ручном вводе мешал
  перехват фокоса Ableton) ✅
- Качество 1080P→720P ✅ (панель показала реальные высоты 2160/1440/1080/720)
- Контейнер MP4→WEBM ✅ (панель MP4/WEBM/MKV)
- СКАЧАТЬ ✅, дубликат-модалка ✅ (ОТМЕНА и СКАЧАТЬ ВСЁ ЕЩЁ РАЗ проверены)
- [ОЧИСТИТЬ] ✅ (pointerDown→tap→очистка; widget-тест добавлен в набор)
- Корзина строки (удалила файл с диска) ✅; папка строки (Проводник) ✅
- [ССЫЛКА] (браузер) ✅; лого Kvartal и $ — тот же механизм FIX-17
- Кнопка-папка (выбор назначения): нативный диалог проверен ранее ✅
- Кнопки окна «–»/«×», перетаскивание, ресайз ✅
- Пауза/×-строки: ядро покрыто контрактными тестами; залипание воркера
  (первопричина «мёртвых» кнопок) устранено FIX-15; в живом прогоне сеть
  оказалась быстрее клика — живой тест не удался без вреда для качества.

### Тесты после всех правок
- Ядро kd_tests: 45/45 ✅ · Flutter: 9/9 ✅ (включая новые clear_button)

## Часть 1д. Правки по репорту пользователя от 16.09 (вечер)

### FIX-18 · [ССЫЛКА] открывала «Документы» вместо видео
- **Причина:** FIX-17 открывал всё через `explorer.exe <аргумент>`; с URL,
  содержащим `?` и `&`, explorer не понимает схему и открывает «Документы».
- **Стало:** URL → `rundll32 url.dll,FileProtocolHandler` (папки — explorer).
- **Проверено вживую:** клик [ССЫЛКА] → Яндекс.Браузер открыл
  «Me at the zoo — YouTube», Проводник не открывался.

### FIX-19 · После отмены загрузки в папке оставался мусор (.part-хвосты)
- **Причина:** отмена убивала yt-dlp, но свежие `.part`/`.ytdl`/`Frag`-хвосты
  в папке назначения не убирались; на быстрой сети отменённое задание могло
  успеть докачаться целиком — файл оставался «непонятным».
- **Стало:** при завершении «Отменено» движок вычищает свежие хвосты этой
  попытки; защита чужих загрузок: sweep не работает, если рядом качается
  другое задание (его .part неприкосновенен).
- **Проверено:** новый живой тест ядра «отмена чистит .part-хвосты» — ok
  (итог живого прогона: 71 ok / 0 fail). Пояснение по сценарию репорта:
  19-сек ролик качается быстрее, чем человек успевает нажать × — задание
  честно заканчивается done, файл легитимен; это не «продолжение отмены».
  Тестовые файлы из папки «Загрузки\K LOAD» убраны.

## Часть 1е. Правки от 16.09 (ночь): отмена готового + качество

### FIX-20 · «Отменил, а видео скачалось» — × теперь работает и на готовой строке
- **Разбор:** короткий ролик качается 1–2 с — к моменту нажатия × задание
  уже «ГОТОВО», а на готовой строке крестика не было (только папка/корзина);
  нажатие «в пустоту» выглядело как «отмена не работает, файл скачался».
- **Стало (двойная защита):**
  1. Ядро: если флаг отмены выставлен до конца обработки — успевший файл
     удаляется, задание завершается «Отменено».
  2. UI: на готовой строке тоже есть × — убирает файл и строку («передумал»).
- **Проверено вживую:** «Me at the zoo» скачан → клик × на ГОТОВОЙ строке →
  файл исчез из папки, строка очищена.

### FIX-21 · «Плохое качество даже при максимальном»
- **Причина:** yt-dlp без явной сортировки форматов предпочитает совместимые
  avc/h264 — выбирал 1080-avc вместо доступного 2160-vp9.
- **Стало:** `--format-sort res,fps,br,vcodec,acodec,proto,ext` — разрешение
  и fps всегда важнее предпочтений кодека; склейка остаётся ремуксом
  (копией потоков) — без перекодирования и потерь.
- **Проверено вживую:** Big Buck Bunny, чип 2160P → файл
  **3840×2160, кодек vp9, 1.5 ГБ** (ранее тот же прогон давал 134 МБ 1080).
  Файл оставлен в «Загрузки\K LOAD» как образец.

### Сопутствующее (сетевой флак)
- При падении сети/прокси в последний момент задание помечается
  «НЕТ СЕТИ ИЛИ VPN», даже если файл успел лечь целиком — при повторном
  скачивании сработает проверка дублей (файл уже есть).

## Часть 2. Ошибки, НЕ требующие правки кода (окружение)

- **VS Build Tools была недоустановлена** (пакет ASAN не скачался 06.09.2026 — SHA256/BITS
  сбой, похоже на прокси `127.0.0.1:10809`) → flutter build отказывался работать.
  Долечено через `setup.exe modify --add Microsoft.VisualStudio.Workload.VCTools
  --includeRecommended --passive` под UAC (без elevation — Exit 5007).
- Установлено: vcpkg → `C:\dev\vcpkg` (в кириллическом `%LOCALAPPDATA%` vcpkg не работает),
  PowerShell 7.6.6 → `C:\dev\pwsh`, ninja из состава VS Build Tools.
- `flutter analyze` падает на кириллическом пути (LSP-баг Dart SDK) — на окружении,
  не на проекте; `flutter test` работает.
- Известный проекту баг flutter_tools (финализация integration_test, PathNotFoundException)
  воспроизвёлся локально — поэтому e2e-прогон заменён живыми тестами ядра + ручной проверкой.

## Часть 3. Результаты финального тестирования (свежие прогоны)

| Проверка | Результат |
|---|---|
| `kd_tests.exe` (контрактные, после всех фиксов) | ✅ 45 ok / 0 fail |
| `flutter test` (юнит + каталог 14 позиций) | ✅ 7/7 passed |
| `flutter build windows --release` | ✅ exit 0 |
| Живой probe YouTube (титул, высоты) | ✅ ok (до бот-чека) |
| Живой probe SoundCloud / Pinterest / поиск | ✅ ok |
| Живое скачивание YouTube-видео → файл на диске | ✅ «youtube скачан», путь выдан |
| Живое скачивание SoundCloud → mp3 + проверка длительности ffprobe | ✅ ok |
| Пачка из 2 ссылок (mp4+mp3 в папке) | ✅ ok |
| ХРОН-отрезок (скачан + длительность соответствует) | ✅ ok |
| clearFinished / destroy без зависаний | ✅ после FIX-5/6 |
| YouTube без VPN (повторный прогон в конце дня) | ⚠️ бот-проверка «подтвердите доступ» — ограничение YouTube для IP без VPN; с VPN работает |
| Запуск приложения, UI, плашка VPN, разбор ссылки | ✅ проверено визуально |
| Ярлык на рабочем столе | ✅ создан, target проверен |

## Часть 4. Как проверять вручную

1. Двойной клик по ярлыку **«K LOAD»** на рабочем столе.
2. Закройте плашку «VPN ОТКЛЮЧЁН» крестиком (для YouTube VPN лучше включить —
   иначе YouTube может требовать «проверку не робот»; SoundCloud/Pinterest работают и так).
3. Вставьте ссылку — примеры:
   - YouTube: `https://www.youtube.com/watch?v=jNQXAC9IVRw` (19 сек)
   - SoundCloud: `https://soundcloud.com/forss/flickermood`
   - ХРОН: та же YouTube-ссылка → чип «ХРОН» → ОТ `0:00` ДО `0:10`
4. СКАЧАТЬ → файлы появляются в **«Загрузки\K LOAD»**.
5. Журнал для диагностики: `%APPDATA%\K LOAD\engine.log` (теперь реально пишется).

## Часть 5. Не исправлено (задокументировано сознательно)

- Заголовок окна: чек-лист приёмки требует «окно без заголовка», фактически стандартная
  рамка Windows — вероятно, устаревший пункт чек-листа (нужно решение автора).
- Смещение отрисовки после «свернуть → восстановить» (Flutter/Impeller, поверхностный
  слой) — функционально окно работает; требует проверки на чистой машине.
- Обрывок URL (например, `https://www.youtub ` с пробелом) считается отдельной ссылкой —
  смягчено FIX-3 (ошибка реального запуска теперь видна), полная валидация TLD — на усмотрение автора.
- Окружение: сборка возможна только в ASCII-пути (`C:\dev\k-load`); `flutter analyze`
  и vcpkg в кириллических путях не работают — ограничения инструментов, не проекта.

## Где что лежит

- Установленная версия: `C:\dev\k-load\dist\K LOAD\kload.exe` (+ ярлык на рабочем столе)
- Исходники с исправлениями: `C:\Users\Женя\.zcode\workspace\default\k-load`
  (изменены: `kdcore/src/Engine.cpp`, `kdcore/src/kd_process.h`, `kdcore/src/DestResolver.h`,
  `kdcore/tests/kd_tests.cpp`, `app/lib/ui/screen.dart`, `app/test/widget_test.dart`,
  `win/fetch-tools-win.ps1`, `win/build.ps1` — не закоммичены)
- Инструменты: `C:\dev\k-load\core\tools-win`; вспомогательное: `C:\dev\vcpkg`, `C:\dev\pwsh`
